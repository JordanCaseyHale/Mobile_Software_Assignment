package com.example.mobilesoftwareassignment.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel
import com.example.mobilesoftwareassignment.JourneyRepository

class MainViewModel(application: Application) : AndroidViewModel(application) {
    //private var mRepository: ImageRepository = ImageRepository(application)
}